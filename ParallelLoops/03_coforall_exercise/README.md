# Exercise: coforall basics

Goal: Create `numKittens` tasks that each print a unique kitten name and its task id; then modify so only task 1 feeds the sleepiest cat (demonstrate with (`ref` ...) to avoid races).